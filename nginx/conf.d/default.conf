# ===========================================
# Nginx - Configuración del sitio
# Dominio configurado: sicabit.webdeploy.tech
# ===========================================

# ── Upstream servers (contenedores Docker) ──
upstream frontend_upstream {
    server frontend:3000;
}

upstream backend_upstream {
    server backend:3001;
}

# ──────────────────────────────────────────
# Servidor HTTP (puerto 80)
# - Redirige todo a HTTPS
# - Sirve challenge de Certbot
# ──────────────────────────────────────────
server {
    listen 80;
    listen [::]:80;
    server_name sicabit.webdeploy.tech www.sicabit.webdeploy.tech;

    # Challenge de Let's Encrypt / Certbot
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirigir TODO lo demás a HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# ──────────────────────────────────────────
# Servidor HTTPS (puerto 443)
# - SSL con certificados de Let's Encrypt
# - Reverse proxy a frontend y backend
# ──────────────────────────────────────────
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name sicabit.webdeploy.tech www.sicabit.webdeploy.tech;

    # ── Certificados SSL (Let's Encrypt) ──
    ssl_certificate     /etc/letsencrypt/live/sicabit.webdeploy.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sicabit.webdeploy.tech/privkey.pem;

    # ── Configuración SSL recomendada ──
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    # ── OCSP Stapling ──
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;

    # ── Headers de seguridad ──
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    # ── API → Backend (NestJS) ──
    location /api/ {
        proxy_pass http://backend_upstream;
        proxy_http_version 1.1;

        # Headers para el backend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # WebSocket support (para futuro uso)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # ── Next.js archivos estáticos (_next/static) ──
    location /_next/static {
        proxy_pass http://frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;

        # Cache agresivo para archivos estáticos hasheados
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # ── Imágenes optimizadas de Next.js ──
    location /_next/image {
        proxy_pass http://frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Todo lo demás → Frontend (Next.js) ──
    location / {
        proxy_pass http://frontend_upstream;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # WebSocket support (para HMR en desarrollo, futuro uso)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
